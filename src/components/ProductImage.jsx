import React from "react";
import styled from "styled-components";

const Container = styled.div`
  margin-left: 30px;
`;

const ProductImage = () => {
  return <Container>ProductImage</Container>;
};

export default ProductImage;
